# Caption-Action 0.3.24 — cascade audit

## 1. Changes made
- `AudioCapture.drainFifoWindow`: oldest-first accumulation to targetSamples; never newest-trim; optional ~10 s backlog cap (drop oldest, Log.w once). `drainToNewestWindow` **unchanged**.
- `MicTranslatorActivity`: uses `drainFifoWindow(28_000)` + `forceFlush=true`; timing `Log.i(MicTranslator)`; EN skip-MT retained; no cancel/skip on slow ASR.
- `WhisperCppInferenceEngine`: mic threads 2–4; mic EN always `language=en`/`translate=false` (`mic-en-direct`); `MIC_DISCARD_RMS=45`. Playback threads=6 / `FLUSH_AT_PLAYBACK` / playback shouldFlush / Live forceFlush **unchanged**.
- versionName `0.3.24`, versionCode 47.

## 2. Cascade audit table

| File touched | Neighbors audited | Result |
|---|---|---|
| `AudioCapture.kt` (`drainFifoWindow` added) | `CaptionOverlayService` Live loop still calls `drainToNewestWindow`; Mic calls `drainFifoWindow` only | Live newest-trim path **unchanged**. Mic FIFO only. Pump / playback / mic start APIs unchanged. |
| `MicTranslatorActivity.kt` | Home mic gate; `startMicrophoneCapture`; `InferenceEngineFactory` BALANCED; `MlKitTranslationEngine.applyPolicy`; layout/strings | Home / Live / AudioCapture playback untouched. EN skip-MT local to Mic. |
| `WhisperCppInferenceEngine.kt` | Live: `setPlaybackCapture(true)` + `forceFlush=true`; Mic: `setPlaybackCapture(false)` + `forceFlush=true` | Mic-only threads / mic-en-direct / MIC_DISCARD. Playback branch constants + shouldFlush **unchanged**. |
| `CaptionOverlayService.kt` | drain + playbackCapture + forceFlush | **Not modified** — still `drainToNewestWindow` + `playbackCapture=true` + `forceFlush=true`. |
| `SherpaInferenceEngine.kt` | optional SenseVoice | **Not modified** — Live default remains whisper Tiny. |
| `TranslationEngine` / `AsrJunkFilter` | Live applyPolicy / junk | **Not modified**. |
| `README.md` / `docs/physical-test.md` / `docs/0.3.24-cascade-audit.md` | release notes / test | Version + FIFO notes. |
| `app/build.gradle.kts` | versionName / versionCode | `0.3.24` / 47. |

## 3. Live playback path: UNCHANGED — evidence
- `CaptionOverlayService`: still `audioCapture.drainToNewestWindow(windowSamples)` and `setPlaybackCapture(true)` + `transcribeWindow(..., forceFlush = true)`.
- `WhisperCppInferenceEngine` playback: `FLUSH_AT_PLAYBACK = 16_000 * 4`, `PLAYBACK_SPEECH_RMS = 4f`, `PLAYBACK_DISCARD_RMS = 1f`, threads=6, enDirectBias path for Live EN.
- Mic FIFO / mic-en-direct / MIC_DISCARD / mic threads only when `!playbackCapture`.

## 4. APK paths + version
- Version: **0.3.24** (versionCode **47**)
- `/workspace/uploads/caption-action-0.3.24-debug.apk`
- `/workspace/uploads/caption-action-debug.apk`
- `/workspace/uploads/Caption-Action-0.3.24-debug.apk`
